# ВЕНЕМАК

Демонстрационный стенд медицинского AI-ассистента для предварительного анализа медицинских изображений.

> **Важно.** Это proof-of-concept, а не медицинский продукт. Не загружайте персональные данные пациентов. Используйте только обезличенные изображения. Результат анализа не является медицинским заключением.

---

## Технологии

- **Next.js 15** — App Router, Server Components, Route Handlers
- **TypeScript** — строгая типизация, без `any`
- **Tailwind CSS v3** — без сторонних UI-библиотек
- **OpenAI GPT-4o** — мультимодальная модель: ответы по текстовому вопросу и анализ изображений

---

## Функциональность

- Текстовый клинический вопрос (работает и без изображения)
- Опциональная загрузка медицинского изображения (JPG, PNG, WEBP, до 10 МБ)
- Предпросмотр изображения в браузере
- Подтверждение обезличенности изображения (чекбокс, требуется только при загрузке файла)
- Структурированный ответ AI в формате ВЕНЕМАК
- Копирование ответа в буфер обмена
- Опциональная защита доступа по токену (`?token=...`)
- Rate limit: 100 запросов в сутки на весь стенд (in-memory)

---

## Структура проекта

```
src/
├── app/
│   ├── page.tsx                  # Server Component: проверка токена доступа
│   ├── main-content.tsx          # Client Component: весь интерфейс
│   ├── layout.tsx                # Шрифт Inter + метаданные
│   ├── globals.css
│   └── api/
│       └── analyze/
│           └── route.ts          # POST /api/analyze — OpenAI + валидация + rate limit
├── lib/
│   └── venemakPrompt.ts          # System prompt ВЕНЕМАК
└── types/
    └── index.ts                  # AnalyzeResponse, AnalyzeError
```

---

## Быстрый старт

### 1. Установить зависимости

```bash
npm install
```

### 2. Настроить переменные окружения

Скопировать файл примера:

```bash
cp .env.local.example .env.local
```

Открыть `.env.local` и заполнить:

```env
# Обязательный. Получить на platform.openai.com → API keys
OPENAI_API_KEY=sk-...

# Опциональный. Если задан — доступ только по /?token=значение
DEMO_ACCESS_TOKEN=
```

### 3. Запустить в режиме разработки

```bash
npm run dev
```

Открыть [http://localhost:3000](http://localhost:3000)

### 4. Сборка для продакшена

```bash
npm run build
npm run start
```

---

## Переменные окружения

| Переменная | Обязательная | Описание |
|---|---|---|
| `OPENAI_API_KEY` | Да | API-ключ. При прямом доступе — с [platform.openai.com](https://platform.openai.com) → API keys. При работе через провайдера (например, neuroapi.host) — ключ из его кабинета |
| `OPENAI_BASE_URL` | Нет | Эндпоинт OpenAI-совместимого провайдера. Для neuroapi.host — base URL из кабинета (напр. `https://neuroapi.host/v1`). Пусто = прямой `api.openai.com` |
| `DEMO_ACCESS_TOKEN` | Нет | Если задан, страница доступна только по `/?token=значение`. Если не задан — доступ открыт |

---

## Защита доступа

Если задан `DEMO_ACCESS_TOKEN`, открыть стенд можно только передав токен в URL:

```
http://localhost:3000/?token=ваш_токен
```

При отсутствии или неверном токене отображается экран «Доступ запрещён».

---

## Ограничения

| Параметр | Значение |
|---|---|
| Допустимые форматы | JPG, PNG, WEBP |
| Максимальный размер файла | 10 МБ |
| Rate limit | 100 запросов / сутки (сбрасывается при перезапуске сервера) |
| Максимальная длина ответа AI | ~2000 символов |

---

## Что намеренно не реализовано

- Регистрация и авторизация пользователей
- База данных и история запросов
- Сохранение изображений на сервере
- Обратная связь и оценка ответов
- PDF-экспорт
- Мобильное приложение
- Интеграция с МИС

---

## Безопасность и приватность

- Изображение передаётся напрямую в OpenAI API и **нигде не сохраняется**: ни на сервере, ни в базе, ни в браузере
- Вопрос и ответ хранятся только во временном состоянии страницы и пропадают при перезагрузке (никакого `localStorage`)
- Кнопка «Очистить» сбрасывает все данные на странице
- Файл обрабатывается в памяти один раз и не пишется на диск

---

## Лицензия

Демонстрационный проект. Не предназначен для использования в клинической практике.
